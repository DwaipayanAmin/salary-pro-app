import 'package:flutter/material.dart';

class EsiScreen extends StatelessWidget {
  const EsiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ESI Details')),
      body: const Center(child: Text('ESI Details Screen')),
    );
  }
}
